import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Proyecto2doParcialPage } from './proyecto2do-parcial.page';

describe('Proyecto2doParcialPage', () => {
  let component: Proyecto2doParcialPage;
  let fixture: ComponentFixture<Proyecto2doParcialPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Proyecto2doParcialPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
